<h1>View Products by Category: <%= @category.name %> </h1>

<% @products.each do |product| %>
  <li><%= make_thumbnail_link(product) %>
  <%= link_to product.name, product_path(product.id)%> <%= usd(product.price) %></li>
<%end%>